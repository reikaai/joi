We need to build an MCP server using FastMCP. It will be an umbrella for multiple ones - runnable with the same server, all servers should just be prefixed
First, it should wrap tmdb to lookup movies by the imdb and find an imdb of the movie by name. Also, find recommendations and related ones. List movies by genre.
Then, we will create another mcp under the same one